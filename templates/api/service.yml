apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-image-storage
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-image-storage
  ports:
  - port: 80
    nodePort: {{ .Values.ports.backEnd}}
