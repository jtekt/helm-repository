apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-storage.fullname" . }}
  labels:
    {{- include "image-storage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-storage.selectorLabels" . | nindent 4 }}
