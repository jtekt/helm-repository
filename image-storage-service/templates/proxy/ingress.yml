{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "image-storage-service.prefix" . }}-proxy
  annotations:
    {{- with .Values.ingress.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  tls:
    - hosts:
        - {{ include "image-storage-service.ingress.host" . }}
      secretName: {{ include "image-storage-service.prefix" . }}-proxy
  rules:
    - host: {{ include "image-storage-service.ingress.host" . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: 
                name: {{ include "image-storage-service.prefix" . }}-proxy
                port:
                  number: 80
{{- end }}
